services:
  django:
    build:
      context: .
      dockerfile: Dockerfiles/Dockerfile
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - .env
    network_mode: bridge
